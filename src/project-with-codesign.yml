include:
  - project-base.yml

targets:
  Tinkle:
    settings:
      base:
        CODE_SIGN_ENTITLEMENTS: 'Tinkle/Tinkle.entitlements'
      configs:
        debug:
          SWIFT_ACTIVE_COMPILATION_CONDITIONS: 'USE_SPARKLE DEBUG'
        release:
          SWIFT_ACTIVE_COMPILATION_CONDITIONS: 'USE_SPARKLE'
    dependencies:
      - package: Sparkle
