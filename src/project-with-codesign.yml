include:
  - project-base.yml

targets:
  Tinkle:
    sources:
      - path: Tinkle
        compilerFlags:
          - '-DUSE_SPARKLE'
        excludes:
          - 'objc/*'
          - 'Info.plist.in'
    dependencies:
      - framework: Vendor/Sparkle/Sparkle.framework
